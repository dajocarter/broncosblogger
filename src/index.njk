---
layout: default
---

<article class="md:col-span-2 md:justify-self-center">
  <h1 class="text-link mt-0 mb-8 text-4xl font-extrabold">Latest Blog Posts</h1>
  <ul class="mb-5">
    {% for post in collections.blog | reverse %}
      <li class="text-2xl my-2">
        <a href="{{ post.url }}" class="text-link">{{ post.data.title }}</a>
      </li>
    {% endfor %}
  </ul>
  <p>Go to <a href="/blog" class="text-link">the blog</a> for all my posts.<p>
</article>
